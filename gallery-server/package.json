{
  "name": "cullback-gallery-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Self-hosted alternative to gallery-worker — a single Bun process that serves the same desktop API + mobile review UI. Designed to sit behind a Cloudflare Tunnel (or any HTTPS reverse proxy) so the photographer's friends-and-family use case doesn't require a paid Cloudflare R2/KV setup.",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit",
    "build:bin": "bun build --compile --outfile dist/cullback-gallery-server src/index.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "typescript": "~5.8.3"
  }
}
